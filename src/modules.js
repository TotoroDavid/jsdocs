//@ts-check

/**
 * my math library
 * @module myMath
*/

/**
 * add two numbers!!
 * @param {number} x first number
 * @param {number} y second number
 * @returns {number} sum of x and y
 */
const add = (x, y) => x + y

module.exports = {
    add
}